<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de almacenamiento -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Queries para interactuar con el sistema de archivos -->
    <queries>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
        </intent>
    </queries>

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono_aplicacion"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_aplicacion"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity
            android:name=".Login_usuario.Login_user"
            android:exported="false" />

        <activity
            android:name=".Registro_usuario.Registro"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Detalle.Detalle_registro"
            android:exported="false" />

        <activity
            android:name=".Detalle.Detalle_nota"
            android:exported="false" />

        <activity
            android:name=".Detalle.Detalle_tarjeta"
            android:exported="false" />

        <activity
            android:name=".OpcionesPassword.Agregar_Actualizar_Registro"
            android:exported="false" />

        <activity
            android:name=".OpcionesPassword.Agregar_Actualizar_Nota"
            android:exported="false" />

        <activity
            android:name=".OpcionesPassword.Agregar_Actualizar_Tarjeta"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>