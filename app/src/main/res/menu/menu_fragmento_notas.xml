<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Botón de búsqueda de notas -->
    <item
        android:id="@+id/menu_buscar_nota"
        android:title="@string/menu_buscar_nota"
        android:icon="@drawable/icon_buscar_registro"
        app:showAsAction="always"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <!-- Botón de ordenar notas -->
    <item
        android:id="@+id/menu_ordenar_notas"
        android:title="@string/menu_ordenar_notas"
        android:icon="@drawable/icon_ordenar_registros"
        app:showAsAction="ifRoom"/>

    <!-- Botón de número de notas -->
    <item
        android:id="@+id/menu_numero_notas"
        android:title="@string/menu_numero_notas"
        android:icon="@drawable/icon_numero_registros"
        app:showAsAction="ifRoom"/>
</menu>